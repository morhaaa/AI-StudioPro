import React from "react";

interface Props {}

const UserTab: React.FC<Props> = ({}) => {
  return (
    <div className="bg-gradient-to-br from-slate-400 to-slate-700 rounded-md">
      user-tab
    </div>
  );
};

export default UserTab;
